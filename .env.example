# Example env for docker-compose
# Set to 1 to avoid cloning at build time (useful for mounting local code)
SKIP_CLONE=0
# Host port to bind to container port 80
HOST_PORT=80
# Host port to bind to container port 443 (HTTPS)
HOST_PORT_SSL=443
# Set DOMAIN to request a Let's Encrypt certificate for that hostname.
# If empty, a self-signed certificate will be generated.
DOMAIN=example.com
# Optional email for Let's Encrypt registration. If empty, registration will
# proceed without email (not recommended for production).
LETSENCRYPT_EMAIL=

# Challenge method for Let's Encrypt: http-01 or dns-01
LETSENCRYPT_CHALLENGE=http-01

# Use Let's Encrypt staging (0 or 1). Staging avoids rate limits but issues test certs.
LETSENCRYPT_STAGING=0

# Optional additional hostnames (comma-separated). Example: www.example.com,api.example.com
ADDITIONAL_DOMAINS=

# DNS-01 configuration (used when LETSENCRYPT_CHALLENGE=dns-01)
# Choose your DNS provider plugin (e.g., cloudflare, route53, digitalocean, google, linode, ovh)
LETSENCRYPT_DNS_PROVIDER=
# Path inside the container to the provider credential file (mount via ./secrets)
# Example for Cloudflare: /secrets/cloudflare.ini
LETSENCRYPT_DNS_CREDENTIALS=
# Propagation wait time in seconds (provider-specific). Default 60
LETSENCRYPT_DNS_PROPAGATION_SECONDS=60
# Advanced: extra flags to pass to certbot for DNS providers (space-separated)
# Example (Route53): --dns-route53
LETSENCRYPT_DNS_PROVIDER_OPTS=

# Convenience: Cloudflare token (used when LETSENCRYPT_DNS_PROVIDER=cloudflare)
# If set and LETSENCRYPT_DNS_CREDENTIALS is empty, the container will create
# /etc/letsencrypt/cloudflare.ini with this token and chmod 600, then use it.
DNS_CLOUDFLARE_API_TOKEN=
